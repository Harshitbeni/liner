/** Managed OpenCode engine — re-export for stable import path. */
export {
  applyEngineEnv,
  startManagedEngine,
  stopManagedEngine,
  isManagedEngineEnabled,
  type ManagedEngineResult,
  type StartManagedEngineOptions,
} from './opencode-supervisor';

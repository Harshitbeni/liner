name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version tag (e.g. v0.2.0)'
        required: true
        default: v0.2.0

jobs:
  build-desktop:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Build desktop artifact (bundled OpenCode engine)
        run: bun run build:desktop:bundled
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: liner-desktop-${{ github.event.inputs.version }}
          path: |
            apps/liner-electron/release/*.dmg
            apps/liner-electron/release/mac-arm64/Liner.app
          if-no-files-found: warn
